# Nginx configuration for habittracker.akmelias.com
# Save this to: /etc/nginx/sites-available/habittracker

server {
    listen 80;
    listen [::]:80;
    server_name habittracker.akmelias.com;

    # Redirect HTTP to HTTPS (after SSL is set up)
    # Uncomment these lines after getting SSL certificate
    # return 301 https://$server_name$request_uri;
}

# HTTPS server block (will be auto-configured by Certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name habittracker.akmelias.com;
#
#     # SSL certificates (Certbot will add these)
#     # ssl_certificate /etc/letsencrypt/live/habittracker.akmelias.com/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/habittracker.akmelias.com/privkey.pem;
#
#     # Proxy to Next.js app
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # Cache static files
#     location /_next/static {
#         proxy_cache STATIC;
#         proxy_pass http://localhost:3000;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#     }
#
#     location /images {
#         proxy_cache STATIC;
#         proxy_pass http://localhost:3000;
#         add_header Cache-Control "public, max-age=86400";
#     }
# }

# Cache configuration
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=STATIC:10m inactive=7d use_temp_path=off;
